---
trigger: always_on
---

- Follow React functional component patterns with hooks
- Use Tailwind utility classes, avoid custom CSS when possible
- implement porper error boundaries and loading states
- add comprehensive error handling for all async function
- use descriptive variable names and add JSDoc comments for comples function
- always consider accessibility in UI components
- implement optimistic updates for better UX
- Use The "Feature-Based" Approach (The Organized Workshop)
- Every "problem" (problem as problem in the original 43 problem to solve) has 2 main components: A Input and output component (i/o component) and a Explanation component on the right (this is for easy reference) and will now can be refer to as "Px-1" or "Px-2" for "problem x, io component" or "problem x explanation component"
- Every "problem" will now be refer to fully as yPx-1 or yPx-2 (with y defined from 1 - 5 stands for classical, number theory, ... DSA) (so for example "3P1-2" is DES, problem 1, explanation component)